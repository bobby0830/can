version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - backend_db:/app/data
    environment:
      - DATABASE=/app/data/calendar_app.db
      - PYTHONUNBUFFERED=1
    restart: always

volumes:
  backend_db:







